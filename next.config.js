/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'ecommerce.routemisr.com',
      'route-mango.vercel.app',
      'img.freepik.com',
      'images.unsplash.com',
    ],
  },
}

module.exports = nextConfig
